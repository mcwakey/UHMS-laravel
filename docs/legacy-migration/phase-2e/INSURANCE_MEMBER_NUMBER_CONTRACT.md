# Insurance member-number contract

**Confirmed exact-comparison diagnostics:** 17,029 rows are blank. Exact case-sensitive, punctuation-preserving comparison identifies 1,019 duplicate groups / 2,555 rows, 1,002 cross-patient groups / 2,521 rows, 255 cross-provider groups / 621 rows and 14 same-patient/provider groups / 28 rows. These group diagnostics overlap. The disjoint precedence partition is 17,029 blank + 2,516 cross-patient duplicate + 18 cross-provider/same-patient duplicate + 16 same-patient/provider duplicate + 5 placeholder + 11,723 valid single-scope = 31,307.

Preserve evidenced text after strict Latin1 decoding, NFC and outer-trim validation; retain protected raw provenance. Never numeric-cast, remove leading zeros, invent or truncate. Case and punctuation remain significant and are compared exactly. Any broader provider-specific normalization requires separately evidenced approval. Blank maps to nullable/not evidenced and is distinct from missing provider. Cross-patient conflicts never cause patient merges or reassignment. The exact-comparison source partition is evidence-complete; target conflicts remain a protected Phase 3 dry-run measurement because target identifiers and values cannot be published.

The protected source-membership token is a domain-separated HMAC over typed, length-prefixed schema/table/source-key and snapshot identity; a separate content token covers all installed fields. The display-safe representation is an opaque run-local exception/reference plus low-cardinality provider/type class—never a member number fragment or raw key. Member-number tokens are matching diagnostics only: member number alone never matches a patient or existing membership.
